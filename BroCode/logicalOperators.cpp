#include <iostream> 

int main() 
{
    // && = check if two conditions are true (AND)
    // || = check if at least one is true (OR)
    // ! = reverses the logical state of its operand (NOT)


    return 0;
}